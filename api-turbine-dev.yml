management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS


turbine:
  appConfig: api-rest-doctor,api-rest-patient,api-rest-auth
  aggregator:
    clusterConfig: default
  clusterNameExpression: new String("default")
